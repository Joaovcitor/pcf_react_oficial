import styled from 'styled-components';

export const MapaWrapper = styled.div`
  width: 100%;
  height: 100vh; /* Isso garante que o mapa ocupe toda a altura da janela */
  position: relative; /* Para manter o mapa fixo no layout */
`;
